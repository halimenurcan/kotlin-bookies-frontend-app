<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.frontendbook.ui.search.SearchActivity">


    <!-- Arama Başlığı -->
    <LinearLayout
        android:id="@+id/searchHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="?android:attr/windowBackground"
        android:elevation="4dp">

        <TextView
            android:id="@+id/searchTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Search"
            android:textSize="24sp"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/searchInput"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="8dp"
            android:hint="Find books, authors, reviews..."
            android:background="@android:drawable/editbox_background"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:drawableStart="@android:drawable/ic_menu_search"
            android:drawablePadding="8dp"
            android:inputType="text" />
    </LinearLayout>


    <!-- İçerik -->
    <LinearLayout
        android:id="@+id/browseContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_marginTop="100dp"
        android:padding="16dp">

        <!-- Browse by başlığı -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Browse by"
            android:textSize="18sp"
            android:textStyle="bold"
            android:paddingBottom="8dp" />

        <!-- Filtre Butonları -->
        <Button android:id="@+id/genreButton" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="Genre" />
        <Button android:id="@+id/authorButton" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="Author" />
        <Button android:id="@+id/countryButton" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="Country" />
        <Button android:id="@+id/languageButton" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="Language" />
        <Button android:id="@+id/releaseDateButton" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="Release Date" />
        <Button android:id="@+id/mostPopularButton" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="Most Popular" />
        <Button android:id="@+id/highlyRatedButton" android:layout_width="match_parent" android:layout_height="wrap_content" android:text="Highly Rated" />

        <!-- Filtreye göre kullanıcı girişi -->
        <EditText
            android:id="@+id/browseInput"
            android:visibility="gone"
            android:hint="Enter value..."
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp" />

        <Button
            android:id="@+id/browseSubmitButton"
            android:visibility="gone"
            android:text="Search"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <!-- AI Search Butonu -->
        <Button
            android:id="@+id/aiSearchButton"
            android:text="AI Search"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="8dp" />

        <!-- Kitap listesi -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:visibility="gone"
            android:layout_marginTop="16dp"
            android:clipToPadding="false"
            android:paddingBottom="8dp"
            android:contentDescription="Book results list" />
    </LinearLayout>

    <!-- Footer -->
    <fragment
        android:id="@+id/footerFragment"
        android:name="com.example.frontendbook.ui.components.FooterFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        tools:layout="@layout/footer_nav_menu" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>
